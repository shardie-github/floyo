{
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 60000,
    "hideElements": ".skip-link, [aria-hidden='true']",
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-setuid-sandbox"]
    }
  },
  "urls": [
    "http://localhost:3000/",
    "http://localhost:3000/integrations"
  ]
}