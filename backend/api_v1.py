"""API v1 routes - versioned API structure."""

# This file can be used to organize v1 API routes separately
# For now, we'll use a simple versioning strategy with URL prefix

# Version prefix: /api/v1/...
# Default routes without prefix continue to work for backward compatibility
